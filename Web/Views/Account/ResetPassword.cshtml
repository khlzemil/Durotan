@{
    Layout = null;
    ViewBag.Title = "Reset Password";
    var returnUrl = Context.Request.Query["returnUrl"];
}

@using Web.ViewModels.Account;
@model ResetPasswordVM;


    <section class="login-sec first-sec">
        <div class="login-div">
            <div class="login-right">
                <div class="form-heading">
                    <h2 class="form-tit">Returning Customer</h2>
                    <p class="form-tit2">I am a returning customer</p>
                </div>
                <form asp-action="resetpassword" asp-controller="account" method="post">
                    <div class="row">
                        <div asp-validation-summary="ModelOnly" class="text-danger"></div>
                        <div class="col-12">
                            <div class="form-group">
                                <label style="display:none" asp-for="@Model.Password"></label>
                                <input asp-for="@Model.Password" class="form-control-1" placeholder="Password *">
                                <span asp-validation-for="@Model.Password"></span>
                            </div>
                            <div class="form-group">
                                <label style="display:none" asp-for="@Model.ConfirmPassword"></label>
                                <input asp-for="@Model.ConfirmPassword" class="form-control-1" placeholder="ConfirmPassword *">
                                <span asp-validation-for="@Model.ConfirmPassword"></span>
                            </div>
                        </div>
                        <input type="hidden" asp-for="Token" value="@Model.Token" />
                        <input type="hidden" asp-for="Id" value="@Model.Id" />
                    </div>
                    <div class="form-group">
                        <button type="submit" class="btn submit-btn">Reset Password</button>
                    </div>
                </form>
            </div>
        </div>

    </section>


